{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstantiateMsg",
  "type": "object",
  "required": [
    "admin_address"
  ],
  "properties": {
    "admin_address": {
      "description": "Contract admin - can delete any user's polls this is a string because we will validate & save later",
      "type": "string"
    }
  }
}
